# §3.5.2 代码内合成 · §3.6 策略

> 本文对应原文 §3.5.2 与 §3.6，§ 编号与原文一致，未重编号。

### 3.5.2 [ADD] 代码内合成（官方 Step 7，最易漏）

逐个判断归入（可多选）：**加权和**（写权重与来源）/ **硬规则**（如「任一严重违规即拦截」，加权和表达不了，必须独立条件）/ **保留独立信号**（供后续改权重、改显示筛选）/ **下游 ML 特征**（概率作为经典模型特征）。

写明：证据与问题含义未变时，改权重或改显示筛选**不需重跑推理**。

## 3.6 策略

**[ADD] 阈值契约表**（每行四列填满，缺列即未完成）：

| 判断 | 阈值 | 基线数据（来源 + 条数） | 两类错误代价（误放 / 误拦） | 临界区动作 |
|---|---|---|---|---|

- 动作列须覆盖官方 Step 8 的两条路：**转人工** 与 **升级到更贵的推理模型**（不要只写转人工）。本表动作名与 §1.2 描述必须一致。
- [ADD] **无基线数据时不要编**：写 `placeholder`，并给标定方案——官方 Step 8 的方法：**在自有数据上画 confidence-vs-accuracy 曲线**定阈值；上线前先以记录模式收集分布，再按分位点取阈。

**[ADD] confidence 与 probability 分离**（`agent-skill.md` common issues）：

- 只要选最优 → 取 confidence 最高者（argmax），**不设阈值**。
- 要统计算法 / 加权特征 → 用 **probability**。
- 全文不得写成「概率 / confidence」。
