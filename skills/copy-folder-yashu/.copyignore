# ==========================================
# copy-folder-yashu 黑名单配置文件 (.copyignore)
# ==========================================
# 每行一个要忽略的文件或文件夹**名称**（不是路径），
# 在任意层级命中同名条目即跳过整棵子树，大小写不敏感。
#
# 规则：
#   - 以 # 开头的行为注释，会被忽略
#   - 空行会被忽略
#   - 只写名称，不要写路径（如写 .git，不是 src/.git）
#   - node_modules 由脚本始终排除，无需写在这里
#
# 示例：
#   .git          跳过所有 .git 目录
#   dist          跳过所有 dist 目录
#   secret.txt    跳过所有名为 secret.txt 的文件
# ==========================================

# --- 版本控制 ---
.git
.svn
.hg

# --- 构建产物 ---
dist
build
output

# --- 缓存 / 临时目录 ---
.cache
.temp
.tmp

# --- 敏感 / 密钥文件 ---
.env
.env.local
secret.txt
credentials.json
*.pem
*.key

# --- IDE / 编辑器 ---
.vscode
.idea


.vscode
